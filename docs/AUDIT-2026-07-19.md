# Adversarial Audit — 2026-07-19

Five parallel audit dimensions (performance, code quality, logic, resilience,
usability) run against `2fbba20`. Top-severity findings were independently
re-verified against source before being accepted. This document records the
verdict, what the same-day fix bundle addressed, and the deferred queue.

## Fixed in this bundle (tiers 1-5)

| Tier | Finding                                                                                                                                                                                                                                                             | Fix                                                                                                                                                                                                                                                                                                                                        |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1    | Dual Preact instances (pinned 10.24.3 vs floating `@10` → CDN drift already at 10.29.7) + hard CDN dependency in a self-hosted app                                                                                                                                  | Vendored preact/htm/hls.js under `client/public/vendor/`, import map in `index.html`, bare specifiers everywhere; CDN hosts dropped from CSP                                                                                                                                                                                               |
| 2    | No global error handlers; transcoder spawn had no `error`/`exit` handler (any Play could kill the server); `serveDirectFile` fire-and-forget (deleted file → process death); cleanup + heartbeat timers threw into the void                                         | `uncaughtException`/`unhandledRejection` log-don't-die handlers; `TranscodeJob.exited/exitCode` + handlers; waits reject fast on dead encoders; `serveDirectFile` awaited with ENOENT→404; both timer loops guarded                                                                                                                        |
| 3    | Eager 4-profile ffmpeg fan-out per Play; ~10 GB disk per session, no free-space check; orphaned transcode dirs never swept; client only tore down sessions on the Back button                                                                                       | Lazy per-profile start on first playlist request; `VLMP_MAX_TRANSCODE_SESSIONS` cap (default 4, direct play uncapped); `statfs` free-space floor (`VLMP_MIN_FREE_DISK_MB`, default 2 GB) at session + job start; boot-time transcode-dir sweep; Player tears down via unmount + `pagehide` keepalive fetch                                 |
| 4    | `/stream/sessions` (capability-token list) visible to any logged-in user                                                                                                                                                                                            | `adminOnly`                                                                                                                                                                                                                                                                                                                                |
| 5    | HLS resume double-seek + progress saved on the wrong timeline; federated playback broken in both modes (playlist.m3u8 vs master.m3u8; direct URL unrewritten); HMAC verified query-stripped URL the client had signed with query; heartbeat mutual-offline deadlock | Client timeline-offset model (`startOffsetRef`); relative master-playlist URIs (proxy needs no rewriting — `rewritePlaylist` removed); streaming Range-forwarding proxy (no 10s abort on direct); HMAC signs/verifies full URL + exact body mirror; heartbeat admits + reactivates `offline` peers (allow-list: never `pending`/`revoked`) |

Also folded from the ship audit (R1+R2): lazy-start double-spawn race guard,
`startOffsetRef` reset + init-window save gate in Player.js, upstream-abort on
client disconnect for the federation stream proxy, `parseJsonColumn` guard for
the 4 unguarded `JSON.parse(media.audio_tracks)` sites.

Tests: 173 → 181 (new `federation-seams.test.ts` integration-tests the
sign/verify seam through real routes; session cap + lazy-options + relative
master playlist covered). Browser smoke test of the vendored import map passed
(login renders, hooks live).

## Audit scoreboard (ship gate)

- Bundle: working tree vs `2fbba20`, 35 files, ~+650/−330
- Rounds: R1 (adversarial) + R2 (fix-the-fix + adjacent-surface sweep) — Tier 1
- R1: 0 Critical, 4 Warning — 3 fixed in-bundle (1 was already fixed mid-audit), 1 accepted (pagehide vs bfcache tradeoff)
- R2: 0 Critical, 2 Warning — both fixed in-bundle; sweep of fire-and-forget/spawn-handler/timer/NaN-guard classes across all touched files came back clean
- Pre-existing bugs found by ship audit: 1 (JSON.parse class) · Bundle-regression catches: 4
- Verdict: **PASS** — closure-ready

## Deferred queue (from the same audit — untriggered items are silent backlog)

| Finding                                                                                                                                                             | Severity | Trigger                                                        |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | -------------------------------------------------------------- |
| Guest stream-start consumes a view before media/file checks; no body schema (`playback.ts` guest route)                                                             | High     | Next playback-route change, or first real guest-pass complaint |
| `/admin/health` recomputes full-library audit per request (10k `fs.access`, N+1 duplicates, multi-MB JSON)                                                          | High     | Before any library >1k items                                   |
| Missing FK indexes on `watch_progress/playlist_items/user_preferences/guest_passes(media_id)` — folder delete freezes event loop                                    | High     | Before any library >1k items                                   |
| ~~ffprobe has no timeout; scan runs inline in the HTTP request; stale `scan_status='scanning'` never reset~~ ✅ CLOSED 2026-07-20 v0.1.2 (timeout + background scan + boot reset)                                                            | High     | Next scanner change                                            |
| Keyboard/touch accessibility: app is mouse-only; player controls hover-only; mobile nav hidden ≤768px                                                               | High     | UI redesign (Lumiere/Oxide implementation)                     |
| Browse renders server outage as "no media" (catch → empty rows) — empty-state settings link ✅ FIXED 2026-07-19 (Settings page shipped); outage-vs-empty still open | Medium   | UI redesign                                                    |
| ~~Rescan is add-only (renames duplicate, deletions leave dead rows)~~ ✅ CLOSED 2026-07-19 — empty-trash on scan (`pruneMissingFiles`)                              | —        | done                                                           |
| Classifier: `1920x1080` parses as S20E108 (STILL OPEN — EPISODE_PATTERN matches inside resolutions); ~~leading-year titles take wrong year~~ ✅ half-CLOSED 2026-07-20 v0.1.3 (junk-cut + last-year preference)                                                                                      | Medium   | Next scanner change                                            |
| `<360p` sources needing transcode get empty profile list → unplayable                                                                                               | Medium   | Next playback-route change                                     |
| Pause >10 min reaps the session (no client keepalive) → resume hard-fails                                                                                           | Medium   | Next playback-route change                                     |
| 3 querystring `limit` parsers with 3 failure behaviors (`500`/default/empty)                                                                                        | Medium   | Next routes change                                             |
| Shutdown closes DB before `app.close()` drains in-flight requests                                                                                                   | Medium   | Next index.ts change                                           |
| No DB `integrity_check` at boot, `schema_version` never compared (backup story ✅ CLOSED 2026-07-19 — scheduled `db.backup()`)                                      | Medium   | Before v0.2 tag                                                |
| Playlist reorder subset → 500/false-success; preferences FK 500 vs sibling 404; dead audio-track selector; playlist mutations silent-fail                           | Medium   | Next relevant surface change                                   |
| `doc_series` tables dead; `tv_shows.folder_path` stores title not path                                                                                              | Low      | If doc-series support is ever wanted                           |
| Rate limit mis-scoped (429s households; permits 120 session starts/min)                                                                                             | Low      | Next index.ts change                                           |
| `scanner/watcher.ts` is unwired dead code — `watchLibraryFolder` has zero callers (Windows-port R2, 2026-07-20); wire it up or delete it                            | Low      | If auto-scan (Plex-review adopt) is implemented                |
| ~~`POST /admin/folders` never checks the folder exists~~ ✅ CLOSED 2026-07-20 v0.1.2 (400 on missing dir; discoverMedia also errors on unreadable ROOT)                          | Medium   | Next library-routes change                                     |
| Uninstaller has no running-instance probe — `RMDir /r` silently leaves a locked `node.exe` behind, and the next install aborts on the leftovers (field-hit 2026-07-20, smoke step 13→16)     | Medium   | Next installer change (v0.1.2)                                  |
| A scan promise that HANGS (never settles) with the process still up has no recovery — boot reset only covers restarts; a per-file watchdog exists (ffprobe timeout) but no whole-scan deadline (R2 2026-07-20)                              | Low      | Next scanner change                                             |
| **CPU pegged ~98% on all cores during one 720p playback** (field 2026-07-20, 9800X3D). Root cause candidates, both confirmed present in code: (a) `streaming/transcoder.ts:28-77` spawns ffmpeg with NO `-re` and NO `-threads` cap, so it races to transcode the WHOLE file at max speed across every core instead of staying ~realtime ahead of the player — HLS only needs segments faster than playback, not 20x; (b) `metadata/thumbs.ts:38` dedupes in-flight work per mediaId but has NO GLOBAL concurrency cap, and `Browse.js` CategoryGrid now renders 60 cards at once ⇒ up to 60 concurrent ffmpeg frame-grabs on first view of a poster-less category (regression introduced with the grid in v0.1.3) | High     | NEXT SESSION — before HW transcode work                        |
